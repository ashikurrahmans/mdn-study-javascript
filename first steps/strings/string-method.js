/*
1. starts with method in string - 'startsWith()'
2. starts with method in string - 'endsWith()'
3. Extracting a Subsring form a string  using - 'slice() [suru kotha theke , kontar ag porjonto]
4. changing the case of string - 'toLowerCase()'
5. changing the case of string - 'toUpperCase()'
6. Modify a string - 'replace()'





*/

// Full Details About Strings 

/*

/*
1. starts with method in string - 'startsWith()'
(Exp) => 

const checkIncludes = "Mozilla Firefox"

if(checkIncludes.startsWith('Fire')){
    console.log("This is Found")
}else{
    console.log("This is not Found")
}


========================================

2. starts with method in string - 'endsWith()'
(Exp) => 

const checkIncludes = "Mozilla Firefox"

if(checkIncludes.endsWith('Fire')){
    console.log("This is Found")
}else{
    console.log("This is not Found")
}


========================================

3. Extracting a Subsring form a string  using - 'slice()
(Exp) => 
 const myStr = "MozilaFirefox Chino"
 myStr.slice(6,10) // First index (starting) , second is last er ager ta !!! 

 const slices = ("My dear love, how are you?").slice(10,15)
 console.log(slices)

 ===================================================
4. changing the case of string - 'toLowerCase()'
(Exp) => 
cont myName = "ashikur rahman"
myName.toUpperCase()


 ===================================================
5. changing the case of string - 'toUpperCase()'
(Exp) => 
cont myName = "Ashikur Rsahman"
myName.toLowerCase()


 ===================================================
5. Modifying the string - 'replace()'
(Exp) => 
const browserType = 'mozilla';
const updated = browserType.replace('moz','van');

console.log(updated);      // "vanilla"
console.log(browserType);  // "mozilla"


*/